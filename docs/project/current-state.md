# 当前项目状态

- `investment-portal` 是唯一主项目，使用 Astro + MDX。
- 每日与每周复盘共用 `reviews` 内容集合；正式构建只包含 `public` 状态。
- 笔记本海报生产规范位于 `skills/daily-review-notebook-poster/`。
- 外部工作台只向 `content-inbox/` 提交正式交接包。
- ERP、行业启动和 估值罗盘通过 `src/data/researchTools.ts` 以外链接入。
- 当前不部署、不绑定域名、不接统计、不接交易执行。
